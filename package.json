{
  "name": "playlist-app-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "eslint": "^8.30.0",
    "prettier": "^2.8.0"
  }
}
